# Agent Transition Note

The `Agent/` tree is preserved during the monorepo migration, but it is no longer the primary home for active development.

## Active Destinations

- Service app: `../apps/service`
- Shared agent core: `../packages/agent-core`
- MusicXML library: `../packages/maestroxml`
- Humming detector: `../packages/humming-detector`
- Desktop frontend: `../apps/frontend-desktop`

## Why This File Changed

This file contained unresolved merge conflict markers between the old frontend-focused README and the `maestroxml` package README. It now acts as a transition document so the old path remains present without continuing to accumulate merge conflicts.
